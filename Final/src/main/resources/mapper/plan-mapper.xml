<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="plan">
  <insert id="insertPlan" parameterType="plan">
  	BEGIN
  	INSERT INTO plan VALUES(plan_seq.NEXTVAL, #{planPublic}, #{planWriter}, #{planTitle}, #{planStart}, #{planEnd}, #{planContent}, 0, 0, 0)
  	END
  </insert>
  <select id="selectOnePlan" resultType="int">
  	SELECT
		plan_seq.CURRVAL AS planNo
  	FROM plan
  </select>
  <insert id="insertDay">
  	
  </insert>
</mapper>
